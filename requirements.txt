# =============================================================================
# Aggregated Python dependencies for the CareerGo backend.
# For subsystem-only installs use:
#   - Crawler/requirements.txt          (crawler only)
#   - SkillExtraction/requirements.txt  (skill pipeline only)
#   - api/requirements.txt              (FastAPI only)
# =============================================================================

# ---- Core / shared ----
python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
requests>=2.31.0
tqdm>=4.65.0

# ---- Database ----
psycopg2-binary>=2.9.9

# ---- Crawler (RSS + HTML parsing + retries) ----
feedparser>=6.0.10
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-dateutil>=2.8.0
urllib3>=1.26.0

# ---- SkillExtraction (NER + sentence-transformer + fuzzy) ----
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0

# ---- API (FastAPI + JWT + password hashing) ----
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
passlib>=1.7.4
PyJWT>=2.9.0
pydantic>=2.9.0
